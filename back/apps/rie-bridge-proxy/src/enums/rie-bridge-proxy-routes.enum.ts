/* istanbul ignore file */

// Declarative code
export enum RieBridgeProxyRoutes {
  AUTHORIZATION = '/authorize',
  TOKEN = '/token',
  USERINFO = '/userinfo',
  JWKS = '/jwks',
  END_SESSION = '/session/end',
  WELL_KNOWN = '/.well-known/openid-configuration',
}
