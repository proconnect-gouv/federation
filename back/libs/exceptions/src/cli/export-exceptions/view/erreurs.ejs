# <%= title %>

<% markdown.forEach((library) => { %>

## @fc/<%= library[0].path.split('/')[1] %>
| Code d'erreur | Classe | Code HTTP | error | error_description |
|---|---|---|---|---|<%
library.forEach((errorData) => { const { errorCode, path, exception, http_status_code, error, error_description } = errorData; %>
| [<b><%= errorCode %></b>](<%=projectRootPath%><%=path%>) | [<%= exception %>](<%=projectRootPath%><%=path%>) | <%= http_status_code %> | <%= error %> | <%= error_description %> |<%
})}) %>
