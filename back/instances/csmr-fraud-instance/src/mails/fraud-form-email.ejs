<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN" "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html xmlns="http://www.w3.org/1999/xhtml">
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <title>
      Demande de support - signalement usurpation d’identité
    </title>
  </head>
  <body>
    <p>
      Bonjour,
      <br />
      <br />
      Une demande de support a été envoyée avec les données ci-dessous.
    </p>
    <p>
      <strong>Nom :</strong> <span data-testid="familyName"><%= familyName %></span>
    </p>
    <p>
      <strong>Prénom :</strong> <span data-testid="givenName"><%= givenName %></span>
    </p>
    <p>
      <strong>Date de naissance :</strong> <span data-testid="birthdate"><%= birthdate %></span>
    </p>
    <p>
      <strong>Pays de naissance :</strong> <span data-testid="birthcountry"><%= birthcountry %></span>
    </p>
    <p>
      <strong>Ville de naissance :</strong> <span data-testid="birthplace"><%= birthplace %></span>
    </p>
    <p>
      <strong>Email de communication :</strong> <span data-testid="contactEmail"><%= contactEmail %></span>
    </p>
    <p>
      <strong>Email du compte FI :</strong> <span data-testid="idpEmail"><%= idpEmail %></span>
    </p>
    <% if (phoneNumber) { %>
      <p>
        <strong>Numéro de téléphone :</strong> <span data-testid="phoneNumber"><%= phoneNumber %></span>
      </p>
    <% } %>
    <p>
      <strong>Provenance questionnaire usurpation :</strong> <span data-testid="fraudSurveyOrigin"><%= fraudSurveyOrigin %></span>
    </p>
    <p>
      <strong>Code d’identification de session :</strong> <span data-testid="authenticationEventId"><%= authenticationEventId %></span>
    </p>
    <% if (comment) { %>
      <p>
        <strong>Commentaire de l’usager :</strong> <span data-testid="comment"><%= comment %></span>
      </p>
    <% } %>
    <% if (error) { %>
      <p>
        <strong>Erreur :</strong> <span data-testid="error"><%= error %></span>
      </p>
    <% } %>
    <% if (total) { %>
      <p>
        <strong>Nombre de traces correspondant au code de session :</strong> <span data-testid="total"><%= total %></span>
      </p>
    <% } %>
    <% if (tracks && tracks.length > 0) { %>
      <p>
        <strong>Détails des traces (limité aux 10 premières):</strong>
      </p>
      <table border="1" cellpadding="5" cellspacing="0">
        <thead>
          <tr>
            <th>Date</th>
            <th>Correspondance Account ID</th>
            <th>FI</th>
            <th>FS</th>
            <th>Plateforme</th>
            <th>Pays</th>
            <th>Ville</th>
          </tr>
        </thead>
        <tbody>
          <% tracks.forEach(function(track, index) { %>
            <tr data-testid="track-<%= index %>">
              <td data-testid="date"><%= track.date %></td>
              <td data-testid="accountIdMatch"><%= track.accountIdMatch ? 'Oui' : 'Non' %></td>
              <td data-testid="idpName"><%= track.idpName %></td>
              <td data-testid="spName"><%= track.spName %></td>
              <td data-testid="platform"><%= track.platform %></td>
              <td data-testid="country"><%= track.country %></td>
              <td data-testid="city"><%= track.city %></td>
            </tr>
          <% }); %>
        </tbody>
      </table>
    <% } %>
  </body>
</html>
