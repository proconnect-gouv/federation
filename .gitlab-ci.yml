stages:
  - test

global-test:
  image: node:12.15.0
  stage: test
  script:
    - yarn install
    - yarn lint
    - yarn test --coverage
    - ./scripts/coverage.sh
  coverage: '/Global coverage is: \[([\d\.]+)%\]/'
